name: Main Unity3d to itch.io Workflow

on:
  push:
    branches:
      - 'main'

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Build Unity Project
        uses: ./build-unity-project.yml

      - name: Deploy Unity Project to itch.io
        uses: ./deploy-unity-project.yml
